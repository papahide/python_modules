[tool.poetry]
name = "matrix"
version = "0.1.0"

[tool.poetry.dependencies]
python = "^3.11"
pandas = "^3.0.3"
matplotlib = "^3.10.9"
numpy = "^2.4.5"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"